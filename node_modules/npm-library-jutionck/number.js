const min = (first, second) => {
    if (first < second) {
        return first;
    } else {
        return second;
    }
}

const max = (first, second) => {
    if (first > second) {
        return first;
    } else {
        return second;
    }
}

module.exports = {min, max}
